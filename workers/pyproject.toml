# Configuracao de ferramentas dos workers Screena (Python 3.12).
# Fase 0: apenas config de lint/format. Sem build de pacote, sem dependencias instaladas.

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.black]
target-version = ["py312"]
line-length = 100
