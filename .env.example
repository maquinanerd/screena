# =============================================================================
# Screen — exemplo de variaveis de ambiente
# -----------------------------------------------------------------------------
# IMPORTANTE: chaves e segredos vivem SO em variaveis de ambiente.
#   - NUNCA exponha chaves no frontend / em codigo enviado ao navegador.
#   - NUNCA versione um arquivo .env real (apenas este .env.example entra no git).
#   - Copie este arquivo para .env e preencha com valores reais no ambiente.
# =============================================================================

# PostgreSQL local de desenvolvimento (docker-compose.dev.yml).
# A senha abaixo e placeholder local; troque no seu .env se necessario.
POSTGRES_PASSWORD="screena_dev_password"
DATABASE_URL="postgresql://screena:screena_dev_password@localhost:5432/screena"

# URL publica canonica do site (usada em SEO, sitemap, canonical, schema).
THE_SCREEN_PUBLIC_SITE_URL=https://thescreen.media
# Alias tecnico legado: use apenas se algum script antigo ainda exigir este nome,
# sempre apontando para o dominio atual. Nunca use screena.media como canonico.
# SCREENA_PUBLIC_SITE_URL=https://thescreen.media

# TMDB — usado SO offline por services/ingestion ou workers de sync, nunca no render.
# O client real usa TMDB_READ_ACCESS_TOKEN (v4, preferido) ou TMDB_API_KEY (v3).
TMDB_READ_ACCESS_TOKEN=
TMDB_API_KEY=

# Gemini — IA gera content_blocks SO offline, nunca no render.
GEMINI_API_KEY="your_gemini_api_key_here"
GEMINI_MODEL="gemini-3.1-flash-lite"

# Gemini opcionais — deixe em branco/ajuste apenas no worker offline.
GEMINI_API_BASE_URL=
GEMINI_MAX_RPS=1
GEMINI_MAX_RETRIES=3
GEMINI_BREAKER_THRESHOLD=5
GEMINI_BREAKER_COOLDOWN_MS=30000

# Chave do provedor tecnico de ratings (ex.: RapidAPI) — provider_api != rating_source.
SCREENA_RATINGS_PROVIDER_KEY=

# Chave do provedor tecnico de disponibilidade de streaming (onde assistir) — uso offline.
SCREENA_STREAMING_PROVIDER_KEY=

# Conexao Redis — cache local para leitura no render (sem API externa).
SCREENA_REDIS_URL=redis://localhost:6379

# Ambiente de execucao: development | test | production
NODE_ENV=development
